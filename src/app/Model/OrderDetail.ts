export interface OrderDetail {
    orderID: number;
    orderNumber: string;
    received: string;
    startedDate: string;
    completedDate: string;
    assignedTo: string;
    completedBy: string;
}